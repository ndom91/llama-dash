# Upstream llama-swap base URL (no trailing slash)
LLAMASWAP_URL=

# Skip TLS certificate verification for upstream (true/false)
LLAMASWAP_INSECURE=false

# SQLite database path, relative to project root
DATABASE_PATH=data/dash.db
